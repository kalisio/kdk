import { describe, it, expect, beforeAll, afterAll } from 'vitest'
import { composables } from '../../../client/kdk.client.map.js'

describe('core:composables', () => {
  beforeAll(() => {
    expect(true).toBe(true)
  })

  it('xxx', () => {
    
  })

  afterAll(() => {
    
  })
})
