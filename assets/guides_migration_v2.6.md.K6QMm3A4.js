import{_ as e,c as o,o as a,V as t}from"./chunks/framework.MC2QjGNi.js";const m=JSON.parse('{"title":"v2.6 - Not yet released","description":"","frontmatter":{},"headers":[],"relativePath":"guides/migration/v2.6.md","filePath":"guides/migration/v2.6.md"}'),c={name:"guides/migration/v2.6.md"},s=t(`<h1 id="v2-6-not-yet-released" tabindex="-1">v2.6 - Not yet released <a class="header-anchor" href="#v2-6-not-yet-released" aria-label="Permalink to &quot;v2.6 - Not yet released&quot;">​</a></h1><p>More details can be found in the related <a href="https://github.com/kalisio/kdk/milestone/14" target="_blank" rel="noreferrer">milestone</a> on GitHub.</p><h2 id="major-breaking-changes" tabindex="-1">Major breaking changes <a class="header-anchor" href="#major-breaking-changes" aria-label="Permalink to &quot;Major breaking changes&quot;">​</a></h2><p>💥 Renamed functions in <code>utils.locale.js</code> to resolve confusion between <code>getLocale</code> and <code>getAppLocale</code>, which previously led to inconsistent behavior and locale-related issues.</p><ul><li>Renamed <code>getLocale</code> to <code>getBrowserLocale</code></li><li>Renamed <code>getAppLocale</code> to <code>getLocale</code>.</li><li>Renamed <code>getAppFallbackLocale</code> to <code>getFallbackLocale</code></li></ul><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>Both <code>getLocale</code>and <code>getFallbackLocale</code> have a new signature and allows you to retrieve the full locale in the language-region format (e.g., en-GB). For instance, if the locale is <code>en-GB</code>, then</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>console.log(getLocale()) </span></span>
<span class="line"><span>// =&gt; en</span></span>
<span class="line"><span>console.log(getLocale(false)) </span></span>
<span class="line"><span>// =&gt; en-GB</span></span></code></pre></div></div><p>💥 Renamed <code>KLayersSelector</code> component to <code>KLayersList</code> and added new <code>KLayersSelector</code> component</p><div class="danger custom-block"><p class="custom-block-title">DANGER</p><p>The new <code>KLayersSelector</code> component is NOT the old <code>KLayersSelector</code> component. They are entirely different components, and <code>KLayersList</code> should be now used in place of the old <code>KLayersSelector</code> ❗</p></div><p>💥 Test libs have been updated to work with Puppeteer v24, and Puppeteer has been removed from the KDK:</p><ul><li>Test libs have been moved out of test/client core &amp; map folders and placed at the root of the test directory. Users should update any scripts copying those libraries to applications, in order to not overwrite app-specific test files, or have them detected as tests to be ran</li><li><code>page.waitForTimeout</code>, <code>$x</code> and <code>page.waitForXPath</code> have been deprecated in Puppeteer v24. Any apps still using them must be updated to the following replacements: <ul><li><code>page.waitForTimeout</code> ==&gt; <code>core.waitForTimeout</code> (new util function in the KDK test libs)</li><li><code>$x</code> ==&gt; <code>$$</code> with &#39;xpath/.&#39; as a prefix for the argument</li><li><code>page.waitForXPath</code> ==&gt; <code>page.waitForSelector</code> with &#39;xpath/.&#39; as a prefix for the argument</li></ul></li></ul><p>💥 Removed <code>getColorFromPalette</code> in <code>utils.colors.js</code> in favor of <code>getHtmlColor</code> for consistency and performance.</p><h2 id="major-new-features" tabindex="-1">Major new features <a class="header-anchor" href="#major-new-features" aria-label="Permalink to &quot;Major new features&quot;">​</a></h2><p>👉</p>`,13),n=[s];function l(d,i,r,p,h,u){return a(),o("div",null,n)}const f=e(c,[["render",l]]);export{m as __pageData,f as default};
