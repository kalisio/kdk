import{_ as e,c as t,o,V as r}from"./chunks/framework.MC2QjGNi.js";const m=JSON.parse('{"title":"Mixins","description":"","frontmatter":{},"headers":[],"relativePath":"api/core/mixins.md","filePath":"api/core/mixins.md"}'),i={name:"api/core/mixins.md"},s=r('<h1 id="mixins" tabindex="-1">Mixins <a class="header-anchor" href="#mixins" aria-label="Permalink to &quot;Mixins&quot;">​</a></h1><h2 id="authentication" tabindex="-1">Authentication <a class="header-anchor" href="#authentication" aria-label="Permalink to &quot;Authentication&quot;">​</a></h2><p>Provide basic methods to <strong>register(user)</strong>, <strong>login(email, password)</strong>, <strong>logout()</strong>, and <strong>restoreSession()</strong>.</p><blockquote><p>Will make the currently authenticated user available in the <code>user</code> property of the <a href="./application.html#store">global store</a>.</p></blockquote><h2 id="authorisation" tabindex="-1">Authorisation <a class="header-anchor" href="#authorisation" aria-label="Permalink to &quot;Authorisation&quot;">​</a></h2><p>Compute user&#39; abilities and keeps it up-to-date on user&#39; permissions changes.</p><blockquote><p>Abilities are stored in the <code>user.abilities</code> property of the <a href="./application.html#store">global store</a>.</p></blockquote><h2 id="base-activity" tabindex="-1">Base Activity <a class="header-anchor" href="#base-activity" aria-label="Permalink to &quot;Base Activity&quot;">​</a></h2><p>Make it easier to update the application layout when the user changes his current activity:</p><ul><li><strong>clearTitle()/setTitle()</strong> (un)sets the application bar title in store <code>appBar</code> property</li><li><strong>clearActions()/registerAction(type, action)</strong> (un)registers actions to be used by the activity <ul><li><strong>registerFabAction()</strong> registers actions to be used in action button, <code>type = &#39;fab&#39;</code></li><li><strong>registerTabAction()</strong> registers actions to be used in navigation bar, <code>type = &#39;tabBar&#39;</code></li></ul></li><li><strong>clear/setSearchBar(field, services)</strong> (un)sets the application search bar in store <code>searchBar</code> property</li><li><strong>clear/setLeftDrawer(component, content)</strong> (un)sets the application left drawer in store <code>leftDrawer</code> property</li><li><strong>clear/setRightDrawer(component, content)</strong> (un)sets the application right drawer in store <code>rightDrawer</code> property</li><li><strong>clearActivity()</strong> resets actions/title used by the activity</li><li><strong>refreshActivity()</strong> should be overridden in concrete activities to implement action registration and title - search bar - right panel setup</li></ul><blockquote><p>Causes the activity to be automatically refreshed on user&#39; permissions changes or route change.</p></blockquote><h2 id="base-item" tabindex="-1">Base Item <a class="header-anchor" href="#base-item" aria-label="Permalink to &quot;Base Item&quot;">​</a></h2><p>Make it easier to setup items displayed by a collection:</p><ul><li><strong>clearActions()/registerAction(type, action)</strong> (un)registers actions to be used on the item <ul><li><strong>registerPaneAction()</strong> registers actions to be used in item pane, <code>type = &#39;pane&#39;</code></li><li><strong>registerMenuAction()</strong> registers actions to be used in item menu, <code>type = &#39;menu&#39;</code></li></ul></li><li><strong>refreshActions()</strong> should be overridden in concrete items to implement action registration</li></ul><blockquote><p>Causes the item actions to be automatically refreshed on user&#39; permissions changes.</p></blockquote><h2 id="service-proxy" tabindex="-1">Service Proxy <a class="header-anchor" href="#service-proxy" aria-label="Permalink to &quot;Service Proxy&quot;">​</a></h2><p>Make it easier to access an underlying service from the <strong>contextId</strong> and <strong>service</strong> props:</p><ul><li><strong>getService()</strong> to retrieve the service</li></ul><h2 id="object-proxy" tabindex="-1">Object Proxy <a class="header-anchor" href="#object-proxy" aria-label="Permalink to &quot;Object Proxy&quot;">​</a></h2><p>Make it easier to access an underlying object of a given service from the <strong>objectId</strong> and <strong>perspective</strong> props:</p><ul><li><strong>getObject()</strong> to retrieve the service object</li><li><strong>getObjectId()</strong> to retrieve the service object ID</li><li><strong>loadObject()</strong> causes the service object to be resolved for current properties values</li></ul><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>If a perspective is configured only that perspective will be retrieved.</p></div><div class="danger custom-block"><p class="custom-block-title">DANGER</p><p>The <a href="./mixins.html#service-proxy">service mixin</a> is mandatory when using this mixin.</p></div><h2 id="schema-proxy" tabindex="-1">Schema Proxy <a class="header-anchor" href="#schema-proxy" aria-label="Permalink to &quot;Schema Proxy&quot;">​</a></h2><p>Make it easier to access an underlying <a href="https://json-schema.org/" target="_blank" rel="noreferrer">JSON schema</a> object for a given service from the <strong>schema-name</strong>, <strong>service</strong> or <strong>schema-json</strong> props:</p><ul><li><strong>getSchema()</strong> to retrieve the schema object</li><li><strong>getSchemaId()</strong> to retrieve the schema object ID</li><li><strong>getSchemaName()</strong> to retrieve the schema name</li><li><strong>loadSchema()</strong> causes the schema object to be resolved for current properties values</li></ul><p>If a JSON schema is directly provided (as a string) it will be parsed, otherwise it will load a schema file which name is computed like this:</p><ul><li>basename is the given schema name or service name</li><li>suffix is <code>.update</code> if the <code>objectId</code> props is defined or <code>.create</code> otherwise</li><li><code>-perspective</code> is added to suffix if the <code>perspective</code> props is defined</li><li>extension is always <code>.json</code></li></ul><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>This mixin has been designed to be used with the <a href="./mixins.html#service-proxy">service mixin</a> and the <a href="./mixins.html#object-proxy">object mixin</a>.</p></div><p>For instance, if you set props like this <code>&lt;my-editor service=&quot;users&quot;/&gt;</code> on your component using the mixins, the <code>users.create.json</code> schema file will be loaded. If you set props like this <code>&lt;my-editor service=&quot;users&quot; :objectId=&quot;objectId&quot; perspective=&quot;profile&quot;/&gt;</code>, the <code>users.update-profile.json</code> schema file will be loaded.</p><h2 id="base-editor" tabindex="-1">Base Editor <a class="header-anchor" href="#base-editor" aria-label="Permalink to &quot;Base Editor&quot;">​</a></h2><p>Make it easier to build <a href="./components.html#editors">editors</a> from <strong>baseObject</strong> and <strong>baseQuery</strong> props, as well as props defined on associated mixins:</p><ul><li><strong>getMode()</strong> returns <code>updated</code> or <code>create</code> depending if the <strong>objectId</strong> props is defined or not</li><li><strong>fillEditor()</strong> fill all forms with current object values</li><li><strong>clear()</strong> clear all forms back to default values</li><li><strong>validateForms()</strong> validate all forms</li><li><strong>applyForms()</strong> call <strong>apply()</strong> on all forms</li><li><strong>submittedForms()</strong> call <strong>submitted()</strong> on all forms</li><li><strong>getBaseObject()</strong> return retrieved object from service or input base object as defined in <strong>baseObject</strong> props, if a perspective is defined through the <strong>perspective</strong> props only that perspective is returned.</li><li><strong>getBaseQuery()</strong> return input base query as defined in <strong>baseQuery</strong> props, will automatically add object ID and perspective to query if any defined</li><li><strong>async apply(event, done)</strong> setups all the underlying objects to make it ready for edition</li><li><strong>refresh()</strong> setups all the underlying objects to make it ready for edition <ol><li><a href="./mixins.html#service-proxy">load service</a> from the <strong>contextId</strong> and <strong>service</strong> props</li><li><a href="./mixins.html#schema-proxy">load schema</a> from the <strong>schema-name</strong>, <strong>service</strong> or <strong>schema-json</strong> props</li><li><a href="./mixins.html#object-proxy">load object</a> from the <strong>objectId</strong> and <strong>perspective</strong> props</li><li><a href="./mixins.html#refs-resolver">load form refs</a> from the set of <strong>refs</strong> that have been defined</li><li><a href="./components.html#forms">build forms</a></li><li><a href="./components.html#forms">fill forms</a></li></ol></li></ul><div class="danger custom-block"><p class="custom-block-title">DANGER</p><p>This mixin has been designed to be used with the <a href="./mixins.html#service-proxy">service mixin</a>, the <a href="./mixins.html#schema-proxy">schema mixin</a>, the <a href="./mixins.html#object-proxy">object mixin</a> and the <a href="./mixins.html#refs-resolver">refs resolver mixin</a>.</p></div><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>The <strong>baseObject</strong> props is usually used to keep track of existing or additional &quot;hidden&quot; or &quot;internal&quot; properties in addition to the ones edited throught the form.</p></div><p>Check out a code example <a href="https://github.com/kalisio/kdk/blob/master/core/client/components/editor" target="_blank" rel="noreferrer">here</a> to see how to create your own editors.</p><h2 id="base-field" tabindex="-1">Base Field <a class="header-anchor" href="#base-field" aria-label="Permalink to &quot;Base Field&quot;">​</a></h2><p>Make it easier to build <a href="./components.html#editors">form fields</a> from the <strong>properties</strong> and <strong>display</strong> props:</p><ul><li><strong>emptyModel()</strong> get the default &quot;empty&quot; value of the field, returns an empty string by default</li><li><strong>clear()</strong> set the current value of the field to be the default one if provided through <code>properties.field.default</code>, use &quot;empty&quot; model value otherwise</li><li><strong>value()</strong> get the current value of the field, simply gets the value from model by default</li><li><strong>fill(value)</strong> set the current value of the field, simply copies the value as model by default</li><li><strong>apply (object, field)</strong> applies the current field value on the given target object, simply copies the value in the object by default, to be overloaded if you need to perform specific operations before the form has been submitted</li><li><strong>submitted (object, field)</strong> does nothing by default, to be overloaded if you need to perform specific operations after the form has been submitted</li><li><strong>onChanged()</strong> emits the <code>field-changed</code> event whenever the field value has changed, consequently the form will validate or invalidate the field, should be binded in template to events like <a href="https://quasar.dev/vue-components/input#QInput-API" target="_blank" rel="noreferrer"><code>blur</code></a>.</li></ul><p><a href="https://quasar.dev/vue-components/field" target="_blank" rel="noreferrer">Quasar field components</a> are usually used to implement form fields, the given set of computed properties are available to be bound:</p><ul><li><strong>icon</strong> alias for <code>properties.field.icon</code> if <code>display.icon</code> is <code>true</code>, empty by default</li><li><strong>label</strong> alias for <code>properties.field.label</code> if <code>display.label</code> is <code>true</code>, empty by default</li><li><strong>helper</strong> alias for <code>properties.field.helper</code></li><li><strong>disabled</strong> alias for <code>properties.field.disabled</code>, <code>false</code> by default</li><li><strong>hasError</strong> boolean indicating if a validation error has occured</li><li><strong>errorLabel</strong> alias for <code>properties.field.errorLabel</code>, empty by default</li></ul><div class="tip custom-block"><p class="custom-block-title">TIP</p><p><strong>label</strong>, <strong>helper</strong> and <strong>errorLabel</strong> properties will be automatically internationalized if corresponding values are valid translation keys.</p></div><p>Check out a code example <a href="https://github.com/kalisio/kdk/blob/master/core/client/components/form" target="_blank" rel="noreferrer">here</a> to see how to create your own fields.</p><h2 id="version" tabindex="-1">Version <a class="header-anchor" href="#version" aria-label="Permalink to &quot;Version&quot;">​</a></h2><p>Make it easier to display information about client and API versions in applications:</p><ul><li><strong>refreshVersionNames()</strong> retrieves the version information, it will be stored in <code>clientVersionName</code> and <code>apiVersionName</code> data variables</li></ul>',46),a=[s];function n(l,c,d,p,h,u){return o(),t("div",null,a)}const b=e(i,[["render",n]]);export{m as __pageData,b as default};
